[pytest]
pythonpath = scripts
markers =
    e2e: tests that run the PDF conversion pipeline
    slow: slower tests that may invoke external conversion tools
    known_defect: reproduces a known conversion defect observed on real datasheets; asserts the CORRECT behavior and fails until the converter is fixed (auto-skipped unless --known-defects is passed)
