# Dependencies for scripts/pdf2markdown
# Conversion engine + image post-processing copied from /home/andrei/2026/mdoc.

# PDF -> Markdown conversion engine. Pinned: Task 7 of the UniMERNet plan bumps
# this to 2.107.0 once the suite is green on 2.69.1.
docling==2.69.1

# Image post-processing (table/text/vector detection). docling already pulls in
# opencv, numpy, scipy and Pillow, but we import them directly so pin them here.
opencv-python>=4.8.0   # table grid-line detection (cv2)
numpy>=1.24.0
scipy>=1.10.0          # edge detection for image classification
Pillow>=10.0.0

# OCR via RapidOCR (no system binary required, unlike Tesseract).
rapidocr>=3.0.0
onnxruntime>=1.16.0    # inference backend RapidOCR runs on

# Raster -> vector tracing for diagrams.
vtracer>=0.6.11

# Formula OCR uses UniMERNet-base (both PDF formula regions and HTML equation
# images), from a fork that makes UniMERNet run on a modern transformers so it
# coexists with docling in one venv. transformers is pinned to the version
# verified to run BOTH docling and the fork.
transformers==4.57.6
unimernet @ git+https://github.com/Andrei-Errapart/UniMERNet.git@8dfa160

# HTML parsing for datasheet vendor adapters.
beautifulsoup4>=4.12.0
